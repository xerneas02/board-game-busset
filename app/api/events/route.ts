import { db } from "@/lib/db";import { NextRequest,NextResponse } from "next/server";export const dynamic="force-dynamic";export async function GET(r:NextRequest){const gameId=Number(r.nextUrl.searchParams.get("gameId"));if(!Number.isInteger(gameId))return NextResponse.json({error:"Jeu invalide."},{status:400});return NextResponse.json(db.prepare("SELECT e.id,e.name,COALESCE(SUM(pe.count),0) total FROM game_events e LEFT JOIN play_events pe ON pe.eventId=e.id WHERE e.gameId=? GROUP BY e.id ORDER BY e.name").all(gameId))}export async function POST(r:NextRequest){const {gameId,name}=await r.json(),value=String(name||"").trim();if(!Number.isInteger(gameId)||!value||value.length>60)return NextResponse.json({error:"Événement invalide."},{status:400});try{const row=db.prepare("INSERT INTO game_events(gameId,name) VALUES(?,?)").run(gameId,value);return NextResponse.json({id:row.lastInsertRowid,name:value,total:0},{status:201})}catch{return NextResponse.json({error:"Cet événement existe déjà."},{status:409})}}